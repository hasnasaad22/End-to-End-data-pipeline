weather_dbt:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres_container
      user: airflow
      password: airflow
      port: 5432
      dbname: airflow_db
      schema: bronze
      threads: 4